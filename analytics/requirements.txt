# The forecasting engine intentionally uses only the Python standard library.
# This keeps the local analytics component reproducible in the Replit runtime
# while leaving room for pandas/numpy/scipy-backed models in a later phase.